[package]
name = "aerodesk-bridge"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Cross-PoP media bridge client (#216 M1): viewer on remote PoP + publisher on local PoP, RTP payload passthrough without re-encoding"

[dependencies]
aerodesk-core = { path = "../aerodesk-core" }
aerodesk-protocol = { path = "../aerodesk-protocol" }
serde_json.workspace = true
str0m = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true, features = ["env-filter", "std"] }
