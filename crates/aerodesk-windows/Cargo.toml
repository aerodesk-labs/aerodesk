[package]
name = "aerodesk-windows"
description = "AeroDesk Windows platform adapter compatibility shell (implementation moved to aerodesk-platform)"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]

[target.'cfg(windows)'.dependencies]
aerodesk-platform = { path = "../aerodesk-platform" }

[dev-dependencies]
aerodesk-core = { path = "../aerodesk-core" }
aerodesk-protocol.workspace = true
