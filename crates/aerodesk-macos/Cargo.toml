[package]
name = "aerodesk-macos"
description = "AeroDesk macOS platform adapter compatibility shell (implementation moved to aerodesk-platform)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]

[target.'cfg(target_os = "macos")'.dependencies]
aerodesk-platform = { path = "../aerodesk-platform" }

[dev-dependencies]
aerodesk-core = { path = "../aerodesk-core" }
aerodesk-ffmpeg = { path = "../aerodesk-ffmpeg" }
