name: CI

on:
  push:
  pull_request:

jobs:
  test:
    name: test (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            skip_crates: ""
          - os: ubuntu-latest
            skip_crates: --exclude aerodesk-macos --exclude aerodesk-ios --exclude aerodesk-cli
          - os: windows-latest
            skip_crates: --exclude aerodesk-macos --exclude aerodesk-ios --exclude aerodesk-cli --exclude aerodesk-linux --exclude x264
    runs-on: ${{ matrix.os }}
    env:
      CARGO_TERM_COLOR: always
      # #102：SFU 偶发 Abort 定位——panic 时输出完整回溯
      RUST_BACKTRACE: 1
    steps:
      - uses: actions/checkout@v4

      - name: Checkout str0m (netem dev-dep)
        shell: bash
        run: |
          if [ ! -d "${{ github.workspace }}/../str0m/.git" ]; then
            git clone --depth 1 https://github.com/PulseBeamDev/str0m.git "${{ github.workspace }}/../str0m"
          fi

      - name: System deps (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libfontconfig1-dev libxkbcommon-dev libwayland-dev libx11-dev libxcb1-dev libx264-dev libavcodec-dev libavformat-dev libavutil-dev libswscale-dev libavfilter-dev libswresample-dev libpostproc-dev libavdevice-dev
      - name: System deps (macOS)
        if: runner.os == 'macOS'
        run: brew install x264 ffmpeg

      # #74 Windows FFmpeg 开发库：ffmpeg-sys-next 在 MSVC 下经 vcpkg 或
      # FFMPEG_DIR 定位开发库。此处用 BtbN win64-gpl-shared **8.1** 预编译包
      # （与 ffmpeg-sys-next 8.1.x 的 FFmpeg 8.x API 匹配；gyan.dev 当前仅
      # 提供 9.0，major 版本不匹配有 bindgen/链接风险）。
      # 该 zip 同时提供 MSVC COFF 导入库 lib/*.lib（含 __imp_*）与 MinGW
      # *.dll.a；ffmpeg-sys-next FFMPEG_DIR 分支读 lib/ + include/，我们把
      # bin/ 加进 PATH 供回环测试运行时加载 DLL。zip 用 PowerShell 原生
      # Expand-Archive 解压（windows-latest 无预装 7-Zip，见 runner-images#9361）。
      - name: System deps (Windows FFmpeg dev libs)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ProgressPreference = 'SilentlyContinue'
          $url = "https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-n8.1-latest-win64-gpl-shared-8.1.zip"
          $zip = Join-Path $env:RUNNER_TEMP "ffmpeg.zip"
          Invoke-WebRequest -Uri $url -OutFile $zip
          $dest = Join-Path $env:RUNNER_TEMP "ffmpeg"
          Expand-Archive -Path $zip -DestinationPath $dest -Force
          $root = (Get-ChildItem $dest -Directory | Select-Object -First 1).FullName
          Add-Content $env:GITHUB_ENV "FFMPEG_DIR=$root"
          Add-Content $env:GITHUB_PATH (Join-Path $root "bin")

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: fmt
        run: cargo fmt --all -- --check

      - name: clippy
        run: cargo clippy --workspace ${{ matrix.skip_crates }} --all-targets

      - name: test
        run: cargo test --workspace ${{ matrix.skip_crates }}

      # 端到端冒烟依赖 macOS 专属编码器（aerodesk-cli ← aerodesk-macos），
      # 跨平台编译/单测已由三平台矩阵覆盖。
      - name: e2e smoke (macOS only)
        if: runner.os == 'macOS'
        run: ./scripts/smoke.sh

      # #58 画质选层端到端：publisher simulcast q/h/f → SFU 按 rid 选层，
      # 断言 f 层码率显著高于 q 层（release 构建，避免 debug 失真）。
      - name: e2e simulcast layer switching (macOS only)
        if: runner.os == 'macOS'
        run: ./scripts/simulcast-e2e.sh

      # #8 高码率（大帧）回归：VT 1080p60 --noisy ~5k pps → viewer 关键帧/解码
      # 正常（防 CLI viewer 单包读取丢关键帧回归；VT 不可用时 SKIP）。
      - name: e2e high-rate large-frame (macOS only)
        if: runner.os == 'macOS'
        run: ./scripts/highrate-e2e.sh

      # #66 回归：kill 一个连接后，新连接必须能正常 Join（信号服务器无死锁）。
      - name: e2e signal reconnect after disconnect (macOS only)
        if: runner.os == 'macOS'
        run: ./scripts/signal-reconnect-e2e.sh

      # #58 音频链路：publisher --audio（PCMU）→ SFU → viewer；静音 viewer 丢弃音频。
      - name: e2e audio pipeline (macOS only)
        if: runner.os == 'macOS'
        run: ./scripts/audio-e2e.sh

      # #73 Opus 48kHz：publisher --audio-opus（libopus）→ SFU → viewer 收帧。
      - name: e2e opus audio pipeline (macOS only)
        if: runner.os == 'macOS'
        run: ./scripts/opus-e2e.sh

      # #58 显示器切换：viewer --display N → SFU control 转发 → publisher（控制链路）。
      - name: e2e display switch control path (macOS only)
        if: runner.os == 'macOS'
        run: ./scripts/display-e2e.sh

      # #72 文件传输：publisher --send-file → SFU → viewer --recv-dir，SHA-256 一致。
      # #85：用 release 构建——debug 下 str0m/sctp-proto 高吞吐 data channel
      # 深调用链会 stack overflow（#102 已知，SFU shard 已 8MB 栈缓解）；
      # 吞吐验收本就需 release（LESSON 性能压测）。
      - name: e2e file transfer (macOS only)
        if: runner.os == 'macOS'
        run: PROFILE=release ./scripts/file-transfer-e2e.sh

      # #72 取消回归：publisher --cancel-send-after 中途取消 → viewer 不落盘、无残留。
      # #85：release 构建（debug 深链栈溢出已知限制 #102）。
      - name: e2e file transfer cancel/cleanup (macOS only)
        if: runner.os == 'macOS'
        run: PROFILE=release ./scripts/file-cancel-e2e.sh

      # #72 剪贴板双向同步：viewer→publisher 与 publisher→viewer 两个方向。
      - name: e2e clipboard sync (macOS only)
        if: runner.os == 'macOS'
        run: ./scripts/clipboard-e2e.sh

      # #75 远程光标：publisher 光标通道 → SFU → viewer 收到轨迹。
      - name: e2e remote cursor (macOS only)
        if: runner.os == 'macOS'
        run: ./scripts/cursor-e2e.sh

      # #74 多 codec（H264/H265/VP9/AV1）：--encoder ffmpeg --codec → SFU → viewer 收帧。
      - name: e2e multi-codec (macOS only)
        if: runner.os == 'macOS'
        run: ./scripts/codec-e2e.sh

      # #75 输入注入：viewer 输入事件 → SFU → publisher 注入（macOS CGEvent）。
      - name: e2e input injection (macOS only)
        if: runner.os == 'macOS'
        run: ./scripts/input-e2e.sh

      # #109 远程命令：viewer --run-command → SFU → publisher 执行 → stdout/stderr/exit。
      - name: e2e remote command (macOS only)
        if: runner.os == 'macOS'
        run: ./scripts/cmd-e2e.sh

      # #109 MCP 工具面：stdio JSON-RPC 会话 → aerodesk-mcp → CLI 桥 → 被控端执行。
      - name: e2e mcp tool surface (macOS only)
        if: runner.os == 'macOS'
        run: ./scripts/mcp-e2e.sh

      # #73 A/V 同步：连续视频+音频，AVSYNC 漂移/jitter buffer 统计。
      - name: e2e av sync (macOS only)
        if: runner.os == 'macOS'
        run: ./scripts/avsync-e2e.sh

      # 客户端自测：Web 主控端（浏览器观看 + 输入回传，SFU 内嵌 web/index.html）。
      - name: e2e web viewer (macOS only)
        if: runner.os == 'macOS'
        env:
          BROWSER: chrome
        run: ./scripts/web-e2e.sh

      # 客户端自测：Web 被控端（headless Chrome 屏幕共享发布 → SFU → CLI viewer 收流）。
      - name: e2e web publisher (macOS only)
        if: runner.os == 'macOS'
        env:
          BROWSER: chrome
        run: ./scripts/web-pub-e2e.sh

      - name: windows adapter
        if: runner.os == 'Windows'
        # --examples：编译验证 vdd_smoke（#140，真机验收示例）。
        run: cargo check -p aerodesk-windows --examples
      - name: linux adapter
        if: runner.os == 'Linux'
        run: cargo check -p aerodesk-linux -p aerodesk-ui
      - name: macos/ios adapters + cli
        if: runner.os == 'macOS'
        run: cargo check -p aerodesk-macos -p aerodesk-ios -p aerodesk-cli

  # iOS App 双 slice 构建回归：#14 类链接回归（XCFramework/扁平静态库路径错位）
  # 曾导致 Xcode 无法链接；此处每次 push/PR 都验证模拟器 + 真机 slice 可编译可链接。
  ios-app-build:
    name: iOS app build (sim + device)
    runs-on: macos-latest
    env:
      CARGO_TERM_COLOR: always
    steps:
      - uses: actions/checkout@v4

      - name: Checkout str0m (netem dev-dep)
        shell: bash
        run: |
          if [ ! -d "${{ github.workspace }}/../str0m/.git" ]; then
            git clone --depth 1 https://github.com/PulseBeamDev/str0m.git "${{ github.workspace }}/../str0m"
          fi

      - uses: dtolnay/rust-toolchain@stable

      - name: Add iOS Rust targets
        run: rustup target add aarch64-apple-ios aarch64-apple-ios-sim

      - name: Install xcodegen
        run: brew install xcodegen

      # 产物（XCFramework + xcodeproj）均为 gitignore 生成物，必须现构建/现生成，
      # 与本地“clean clone → build”路径一致（见 docs/PACKAGING.md）。
      - name: Build Rust static libs (sim + device) → XCFramework
        run: bash scripts/build-ios-lib.sh all

      - name: Generate Xcode project
        run: cd ios && xcodegen generate

      - name: Build app (simulator slice)
        run: |
          cd ios
          xcodebuild -project AeroDesk.xcodeproj -scheme AeroDesk \
            -destination 'generic/platform=iOS Simulator' -configuration Debug \
            ARCHS=arm64 ONLY_ACTIVE_ARCH=YES build CODE_SIGNING_ALLOWED=NO

      - name: Build app (device slice, unsigned)
        run: |
          cd ios
          xcodebuild -project AeroDesk.xcodeproj -scheme AeroDesk \
            -destination 'generic/platform=iOS' -configuration Debug \
            ARCHS=arm64 ONLY_ACTIVE_ARCH=YES build CODE_SIGNING_ALLOWED=NO

  # iOS 主控端模拟器自测：#1 观看端全链路（与 SFU 内嵌 web 同协议）——
  # 构建 App → 启动模拟器 → 本地 SFU+x264 发布端 → App 自动连接观看 → 解码帧断言。
  ios-sim-e2e:
    name: iOS simulator e2e (viewer)
    runs-on: macos-15
    env:
      CARGO_TERM_COLOR: always
    steps:
      - uses: actions/checkout@v4

      - name: Checkout str0m (netem dev-dep)
        shell: bash
        run: |
          if [ ! -d "${{ github.workspace }}/../str0m/.git" ]; then
            git clone --depth 1 https://github.com/PulseBeamDev/str0m.git "${{ github.workspace }}/../str0m"
          fi

      - uses: dtolnay/rust-toolchain@stable
      - name: Add iOS Rust targets
        run: rustup target add aarch64-apple-ios aarch64-apple-ios-sim

      - name: Install xcodegen
        run: brew install xcodegen

      - name: System deps (x264 for CLI publisher)
        run: brew install x264 ffmpeg

      - name: Select latest Xcode
        run: |
          XCODE=$(ls -d /Applications/Xcode*.app 2>/dev/null | sort -V | tail -1)
          [ -n "$XCODE" ] && sudo xcode-select -s "$XCODE" || true

      - name: Run iOS simulator e2e
        run: bash scripts/ios-sim-e2e.sh

  # Android APK 构建回归：#2 软件侧（Rust .so + Gradle APK）每次 push/PR 都验证。
  # ubuntu-latest 镜像预装 Android SDK（platform 34 / build-tools / NDK r2x），
  # 动态挑选最新 NDK，避免镜像版本漂移导致脚本 fallback 路径失效。
  android-apk-build:
    name: Android APK build (arm64-v8a)
    runs-on: ubuntu-latest
    env:
      CARGO_TERM_COLOR: always
    steps:
      - uses: actions/checkout@v4

      - name: Checkout str0m (netem dev-dep)
        shell: bash
        run: |
          if [ ! -d "${{ github.workspace }}/../str0m/.git" ]; then
            git clone --depth 1 https://github.com/PulseBeamDev/str0m.git "${{ github.workspace }}/../str0m"
          fi

      - uses: dtolnay/rust-toolchain@stable

      - name: Add Android Rust target
        run: rustup target add aarch64-linux-android

      - name: Install cargo-ndk
        run: cargo install cargo-ndk --locked

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set Android SDK / NDK env
        shell: bash
        run: |
          SDK="/usr/local/lib/android/sdk"
          NDK="$(ls "$SDK/ndk" | sort -V | tail -1)"
          echo "ANDROID_HOME=$SDK" >> "$GITHUB_ENV"
          echo "ANDROID_NDK_HOME=$SDK/ndk/$NDK" >> "$GITHUB_ENV"
          echo "Using Android SDK: $SDK, NDK: $NDK"

      - name: Build Rust .so (arm64-v8a)
        run: bash scripts/build-android-lib.sh

      - name: Build debug APK
        shell: bash
        run: |
          cd android
          # #93：Maven 镜像偶发超时（Gradle 会禁用出错的仓库），重试覆盖。
          for i in 1 2 3; do
            echo "== gradle attempt $i =="
            if ./gradlew assembleDebug --no-daemon; then
              exit 0
            fi
            sleep 15
          done
          exit 1
